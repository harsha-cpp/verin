{
  "name": "verin-dms",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@10.2.1",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "check": "turbo run check",
    "generate:api-client": "pnpm --filter @verin/api-client run generate"
  },
  "devDependencies": {
    "turbo": "^2.5.0"
  }
}
